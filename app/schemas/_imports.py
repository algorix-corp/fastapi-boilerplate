from datetime import datetime, date
from typing import Optional, List

from sqlmodel import Field, SQLModel

from ..utils.LSID import LSID1

Field(), SQLModel(), Optional, datetime.now(), date, LSID1(), List
